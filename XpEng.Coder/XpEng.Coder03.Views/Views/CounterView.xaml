<UserControl x:Class="XpEng.Coder03.Views.Views.CounterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

        <!-- Existing Counter TextBlock -->
        <TextBlock Text="{Binding Count, StringFormat='Current Count: {0}'}" 
                   FontSize="24" 
                   HorizontalAlignment="Center"/>

        <!-- NEW: Binds to the dependent property -->
        <TextBlock Text="{Binding EvenOrOddMessage, StringFormat='(This number is {0})'}" 
                   FontSize="16" 
                   Foreground="Gray"
                   Margin="0,5,0,20" 
                   HorizontalAlignment="Center"/>

        <!-- Existing Button -->
        <Button Content="Click Me" 
                Command="{Binding IncrementCommand}" 
                Width="150" Height="40" />

    </StackPanel>
</UserControl>