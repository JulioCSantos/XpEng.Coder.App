<UserControl x:Class="XpEng.Coder03.Views.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:XpEng.Coder06.ViewModels;assembly=XpEng.Coder06.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewModels:DashboardViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Source Directory Input -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Source Directory:" VerticalAlignment="Center" FontWeight="SemiBold" />
            <TextBox Grid.Column="1" Text="{Binding SourceDirectory, UpdateSourceTrigger=PropertyChanged}" Padding="4" />
        </Grid>

        <!-- Target Directory Input -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Target Directory:" VerticalAlignment="Center" FontWeight="SemiBold" />
            <TextBox Grid.Column="1" Text="{Binding TargetDirectory, UpdateSourceTrigger=PropertyChanged}" Padding="4" />
        </Grid>

        <!-- Template Path Input -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Template Path:" VerticalAlignment="Center" FontWeight="SemiBold" />
            <TextBox Grid.Column="1" Text="{Binding TemplatePath, UpdateSourceTrigger=PropertyChanged}" Padding="4" />
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
            <Button Command="{Binding ForceSyncCommand}" Content="Force Sync" Width="120" Height="30" Margin="0,0,10,0" Cursor="Hand" />
            <Button Command="{Binding ToggleWatchCommand}" Content="{Binding ToggleButtonText}" Width="120" Height="30" Cursor="Hand" />
        </StackPanel>

        <!-- Live Logs Console -->
        <GroupBox Grid.Row="4" Header="Live Logs" FontWeight="SemiBold">
            <ListBox x:Name="LogsListBox" 
                     ItemsSource="{Binding LiveLogs}" 
                     SelectionMode="Extended" 
                     KeyDown="LogsListBox_KeyDown"
                     Margin="5" BorderThickness="0" Background="#FAFAFA" FontFamily="Consolas" FontSize="12">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</UserControl>