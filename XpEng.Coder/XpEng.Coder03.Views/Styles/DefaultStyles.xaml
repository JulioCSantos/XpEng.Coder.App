<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:XpEng.Coder03.Views.Controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="BraceExpanderToggleButtonTemplate" TargetType="ToggleButton">
        <Border x:Name="ButtonBorder" Width="26" Height="26" Background="Transparent"
            BorderBrush="#9CA3AF" BorderThickness="1"
            CornerRadius="4" SnapsToDevicePixels="True">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin=".5,.5">
                <Grid.RenderTransform>
                    <RotateTransform Angle="90"/>
                </Grid.RenderTransform>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Left brace -->
                <Viewbox Grid.Column="0" Width="5" Height="14" Stretch="Fill"
                     HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Path Stroke="Black" StrokeThickness="1.35"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"
                      Data="M 5,0
                            C 2.8,0 2.8,1.8 2.8,3.5
                            C 2.8,5.0 1.0,5.5 0,7
                            C 1.0,8.5 2.8,9.0 2.8,10.5
                            C 2.8,12.2 2.8,14 5,14"/>
                </Viewbox>

                <!-- Expanded content only -->
                <Grid x:Name="ExpandedContent" Grid.Column="1" Width="8" Height="10"
                  Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Ellipse Grid.Row="0" Width="2.1" Height="2.1" Fill="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Ellipse Grid.Row="1" Width="2.1" Height="2.1" Fill="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Ellipse Grid.Row="2" Width="2.1" Height="2.1" Fill="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>

                <!-- Right brace = mirrored left brace -->
                <Viewbox Grid.Column="2" Width="5" Height="14" Stretch="Fill"
                     HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Path Stroke="Black" StrokeThickness="1.35"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      StrokeLineJoin="Round"
                      Data="M 5,0
                            C 2.8,0 2.8,1.8 2.8,3.5
                            C 2.8,5.0 1.0,5.5 0,7
                            C 1.0,8.5 2.8,9.0 2.8,10.5
                            C 2.8,12.2 2.8,14 5,14"
                      RenderTransformOrigin="0.5,0.5">
                        <Path.RenderTransform>
                            <ScaleTransform ScaleX="-1"/>
                        </Path.RenderTransform>
                    </Path>
                </Viewbox>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="ExpandedContent" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#F3F4F6"/>
                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#6B7280"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="ButtonBorder" Property="Background" Value="#E5E7EB"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="ButtonBorder" Property="Opacity" Value=".45"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type Expander}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ToggleButton Grid.Column="0"
                                      IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      Template="{StaticResource BraceExpanderToggleButtonTemplate}"
                                      VerticalAlignment="Center" Focusable="False" Margin="0,0,6,0"/>

                            <ContentPresenter Grid.Column="1" ContentSource="Header"
                                          HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                        </Grid>

                        <!-- Collapsible content -->
                        <ContentPresenter x:Name="ExpandSite" Grid.Row="1" ContentSource="Content"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      Visibility="Collapsed"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value=".55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DimWhenInactiveStyle" TargetType="{x:Type FrameworkElement}">
        <Setter Property="Opacity" Value="1"/>
        <Style.Triggers>
            <Trigger Property="controls:Dimmer.IsActive" Value="False">
                <Setter Property="Opacity" Value=".5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>